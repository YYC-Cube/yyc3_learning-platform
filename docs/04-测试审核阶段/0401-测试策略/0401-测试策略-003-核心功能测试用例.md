# YYC3-Learning-Platform 核心功能测试用例文档

> **文档版本**: v2.0.0  
> **对齐版本**: 当前项目状态 (2026-02-16)  
> **测试范围**: 前端 UI 交互 + 后端 API 端点 + 数据持久化  

---

## 1. 测试分类概览

| 模块 | 测试用例数 | 覆盖组件 |
|------|----------|---------|
| A. 应用启动与初始化 | 6 | App.tsx, apiService.ts |
| B. 导航与路由 | 8 | Sidebar, MobileBottomNav, Header |
| C. 用户仪表盘 | 7 | UserDashboard, Recharts |
| D. 课程模块系统 | 9 | ModulePage, LessonPlayer, ModuleCard |
| E. 看板系统 | 8 | Kanban (DnD) |
| F. 社区系统 | 6 | Community |
| G. 个人中心 | 8 | Profile (Heatmap, Notes) |
| H. 后端 API 端点 | 15 | server/index.tsx |
| I. 响应式布局 | 6 | useResponsive, Mobile components |
| J. 国际化 (i18n) | 4 | LanguageContext |
| **合计** | **77** | |

---

## A. 应用启动与初始化

### TC-A001: 冷启动数据播种 (Seed)

| 项目 | 详情 |
|------|------|
| **前置条件** | KV 存储为空，首次访问应用 |
| **操作步骤** | 1. 打开应用根 URL |
| **预期结果** | - `YYC3API.seed()` 被调用 <br> - 请求体包含 `userProfile` 和 `dashboardStats` <br> - 控制台无错误日志 <br> - 用户头像和名称正确渲染 |
| **验证要点** | 检查 Network 面板中 `/seed` POST 请求返回 `{ success: true }` |
| **优先级** | P0 |

### TC-A002: 后端不可达时的降级启动

| 项目 | 详情 |
|------|------|
| **前置条件** | 边缘函数未部署或不可达 |
| **操作步骤** | 1. 模拟网络断开 <br> 2. 打开应用 |
| **预期结果** | - 控制台输出 `[API Unavailable]` 警告 <br> - 用户看到默认 fallback 数据 (名称 "言语") <br> - 课程列表显示 `updatedMockModules` 数据 <br> - 应用不崩溃，功能可用 |
| **优先级** | P0 |

### TC-A003: 用户资料从 KV 正确加载

| 项目 | 详情 |
|------|------|
| **前置条件** | KV 中已存在 `user:profile:default` 记录 |
| **操作步骤** | 1. 打开应用 |
| **预期结果** | - Header 区域显示正确的用户名和头像 <br> - `user.completionPercentage` 在仪表盘进度条中正确显示 |
| **优先级** | P0 |

### TC-A004: Toaster 通知系统就绪

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 触发任意 toast 操作 (如社区发帖) |
| **预期结果** | - `<Toaster>` 组件在右上角正确渲染 <br> - 主题为 `dark` <br> - 通知动画流畅 |
| **优先级** | P1 |

### TC-A005: Health Check 端点验证

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 浏览器直接访问 `{API_BASE}/health` |
| **预期结果** | 返回 `{ success: true, status: "operational", version: "2.0.0", services: { kv: "connected" } }` |
| **优先级** | P0 |

### TC-A006: Sonner 组件无 next-themes 依赖

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 检查 `/components/ui/sonner.tsx` 导入 |
| **预期结果** | - 无 `next-themes` 导入 <br> - `theme` 硬编码为 `"dark"` |
| **优先级** | P0 |

---

## B. 导航与路由

### TC-B001: 桌面端侧边栏悬停展开

| 项目 | 详情 |
|------|------|
| **前置条件** | 窗口宽度 > 1024px |
| **操作步骤** | 1. 鼠标 hover 侧边栏 <br> 2. 鼠标移出 |
| **预期结果** | - 悬停时: 侧边栏从 `w-20` 展开至 `w-64`，显示文字标签 <br> - 移出时: 平滑收缩回 `w-20`，仅显示图标 <br> - 使用 spring 动画 |
| **优先级** | P1 |

### TC-B002: 移动端底部导航栏

| 项目 | 详情 |
|------|------|
| **前置条件** | 窗口宽度 <= 768px |
| **操作步骤** | 1. 依次点击五个导航项 (概览/课程/看板/社区/我的) |
| **预期结果** | - 蓝色顶部指示线跟随 `layoutId` 动画滑动 <br> - 对应页面内容切换 <br> - `activeTab` 状态正确更新 <br> - 安全区域底部间距正确 (safe-area-inset-bottom) |
| **优先级** | P0 |

### TC-B003: 移动端头部滚动隐藏

| 项目 | 详情 |
|------|------|
| **前置条件** | 窗口宽度 <= 1024px |
| **操作步骤** | 1. 向下滚动超过 100px <br> 2. 向上滚动 |
| **预期结果** | - 向下: 顶部 header 平滑上移隐藏 (`y: -100%`) <br> - 向上: header 重新出现 (`y: 0`) |
| **优先级** | P1 |

### TC-B004: Tab 切换动画

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 从仪表盘切换到看板 <br> 2. 从看板切换到社区 |
| **预期结果** | - 旧内容 fade + slide-left out <br> - 新内容 fade + slide-up in <br> - `AnimatePresence mode="wait"` 确保无重叠 |
| **优先级** | P2 |

### TC-B005: 模块详情导航流

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 在仪表盘点击课程卡片 <br> 2. 进入模块详情 <br> 3. 点击"返回上级" |
| **预期结果** | - `activeTab` 变为 `module-detail` <br> - `selectedModuleId` 被设置 <br> - 返回时恢复 `dashboard` tab |
| **优先级** | P0 |

### TC-B006: 模块详情页 ID 映射

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 传入 moduleId = `'1'` |
| **预期结果** | - `apiService` 中的 `idMap` 映射 `'1'` -> `'ia'` <br> - 找到并渲染 "高级 AI 营销实战" 模块 <br> - 若 ID 无匹配，显示 "未找到模块" |
| **优先级** | P0 |

### TC-B007: 搜索过滤联动

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 在 Header 搜索框输入 "AI" <br> 2. 清除搜索 |
| **预期结果** | - 搜索结果横幅显示匹配数量 <br> - `filteredModules` 仅包含标题/副标题/分类含 "ai" 的模块 <br> - 若当前不在 dashboard/courses 页，自动跳转 <br> - 清除后恢复完整列表 |
| **优先级** | P1 |

### TC-B008: 移动端全屏搜索模态框

| 项目 | 详情 |
|------|------|
| **前置条件** | 窗口宽度 <= 768px |
| **操作步骤** | 1. 点击搜索图标 <br> 2. 输入搜索词 <br> 3. 点击最近搜索标签 <br> 4. 按回车 |
| **预期结果** | - 全屏模态打开，输入框自动聚焦 (200ms 延迟) <br> - 显示 4 个最近搜索和 4 个热门趋势 <br> - 快捷搜索点击后关闭模态并触发搜索 |
| **优先级** | P1 |

---

## C. 用户仪表盘

### TC-C001: 欢迎 Hero 区渲染

| 项目 | 详情 |
|------|------|
| **预期结果** | - 标题"开启您的2026增长之旅"正确渲染 <br> - 渐变文字效果正常 <br> - 进度条动画从 0% 到 68% (1.5s ease-out) |
| **优先级** | P1 |

### TC-C002: 统计卡片网格

| 项目 | 详情 |
|------|------|
| **预期结果** | - 桌面端: 4 列 grid <br> - 移动端: 2 列 grid <br> - 4 张卡片依次以 0.1s 延迟淡入 <br> - 数值: 128 / 42h / Top 2% / 05 |
| **优先级** | P1 |

### TC-C003: 雷达图安全渲染

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 打开仪表盘 |
| **预期结果** | - `useChartReady` 返回 `isReady: true` 后才渲染 RadarChart <br> - 容器宽高均 > 0 <br> - 未触发 "width(0) and height(0)" 控制台错误 <br> - 6 个维度正确显示 |
| **优先级** | P0 |

### TC-C004: 活跃度区域图

| 项目 | 详情 |
|------|------|
| **预期结果** | - 7 天数据正确绘制 <br> - 渐变填充 (indigo 300ms) <br> - Tooltip 显示具体数值 |
| **优先级** | P1 |

### TC-C005: 推荐模块卡片交互

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 悬停课程卡片 <br> 2. 点击卡片 |
| **预期结果** | - 悬停: 卡片上移 10px (`whileHover`) <br> - 图片放大 (`group-hover:scale-110`) <br> - 点击: 导航至模块详情 <br> - 仅显示前 3 个模块 |
| **优先级** | P1 |

### TC-C006: 能力模型下方摘要面板

| 项目 | 详情 |
|------|------|
| **预期结果** | - 显示"最强项: 高并发架构" <br> - 显示"待提升: 数据科学" <br> - "深度报告"按钮可点击 |
| **优先级** | P2 |

### TC-C007: 本周目标与下个里程碑

| 项目 | 详情 |
|------|------|
| **预期结果** | - 本周目标完成度: 92% (emerald 色) <br> - 下个里程碑: "2月7日 架构周" |
| **优先级** | P2 |

---

## D. 课程模块系统

### TC-D001: 模块详情页完整渲染

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 导航至模块详情 (如 ecommerce) |
| **预期结果** | - 缩略图、标题、副标题正确 <br> - 难度标签颜色映射 (初级=emerald, 高级=blue, 专家=indigo) <br> - 核心收获 5 项列出 <br> - 课程目录 15 课时列出 <br> - 导师信息卡渲染 |
| **优先级** | P0 |

### TC-D002: 课程播放入口

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 点击中央 Play 按钮或课程列表项 |
| **预期结果** | - `activeLessonId` 被设置 <br> - 渲染 `LessonPlayer` 组件 <br> - 返回时回到模块详情 |
| **优先级** | P0 |

### TC-D003: LessonPlayer 笔记系统

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 在课程播放页输入笔记 <br> 2. 点击保存 |
| **预期结果** | - 调用 `YYC3API.saveLessonNotes(lessonId, notes)` <br> - KV 存储 `notes_{lessonId}` <br> - 重新进入同一课时时笔记被恢复 |
| **优先级** | P1 |

### TC-D004: 面包屑导航

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 进入模块详情 <br> 2. 检查面包屑 |
| **预期结果** | - 显示: 平台首页 > 商业智库 > {category} > {title} <br> - 预测路径显示 2 个推荐 <br> - 点击预测路径触发 toast |
| **优先级** | P2 |

### TC-D005: 未找到模块的空状态

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 传入无效 moduleId (如 'xyz') |
| **预期结果** | - 显示 "未找到模块" / "Module non trouve" <br> - "返回上级"按钮可点击 |
| **优先级** | P1 |

### TC-D006-D009: 课程列表惰性加载 / 课程卡渐进动画 / 官方推荐标签 / 价格与时长显示 (略)

---

## E. 看板系统

### TC-E001: 初始数据加载

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 切换到看板标签 |
| **预期结果** | - 调用 `YYC3API.getTasks()` <br> - 若 KV 返回空数组: 使用 `initialTasks` (5 条) <br> - Loading spinner 显示然后消失 <br> - 3 列正确渲染 (待处理 / 进行中 / 已完成) |
| **优先级** | P0 |

### TC-E002: 拖拽卡片变更状态

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 拖拽一张 P0 卡片从"进行中"到"已完成" |
| **预期结果** | - 卡片动画移入目标列 <br> - `task.status` 更新为 `done` <br> - `YYC3API.saveTasks()` 自动触发 <br> - 目标列计数更新 <br> - Drop 区域高亮反馈 |
| **优先级** | P0 |

### TC-E003: 拖拽中视觉反馈

| 项目 | 详情 |
|------|------|
| **预期结果** | - 拖拽时: 原卡片透明度降至 0.4 <br> - 目标列: 背景变蓝 (`bg-blue-600/10`), 边框变蓝 <br> - 释放后: 恢复正常外观 |
| **优先级** | P1 |

### TC-E004: 优先级标签样式

| 项目 | 详情 |
|------|------|
| **预期结果** | - P0: 玫红底色 + 玫红文字 <br> - P1: 琥珀底色 + 琥珀文字 <br> - P2: 蓝色底色 + 蓝色文字 |
| **优先级** | P2 |

### TC-E005: 统计卡片数据准确性

| 项目 | 详情 |
|------|------|
| **预期结果** | - P0 任务数: 动态计算 `tasks.filter(t => t.priority === 'P0').length` <br> - 显示为两位数 (padStart) |
| **优先级** | P1 |

### TC-E006: 空列占位 UI

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 将所有卡片拖出某一列 |
| **预期结果** | - 显示虚线边框空状态 <br> - 中央 Plus 图标 <br> - "暂无挂起任务"文案 |
| **优先级** | P2 |

### TC-E007: 保存失败 Toast 通知

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 模拟 API 保存失败 |
| **预期结果** | - 显示 "保存任务失败" 红色 toast |
| **优先级** | P1 |

### TC-E008: 视图切换 (Grid/List)

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 切换到 List 视图 |
| **预期结果** | - `viewMode` 状态更新 <br> - 按钮高亮切换 <br> - (注: 当前 List 视图未实现，仅按钮状态生效) |
| **优先级** | P2 |

---

## F. 社区系统

### TC-F001: 帖子加载与 fallback

| 项目 | 详情 |
|------|------|
| **预期结果** | - 调用 `YYC3API.getPosts()` <br> - 空数据时使用 `initialPosts` (2 条) <br> - 帖子卡片正确渲染 (头像、内容、标签、时间) |
| **优先级** | P0 |

### TC-F002: 发布新帖

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 输入内容 <br> 2. 点击"发布动态" |
| **预期结果** | - 新帖插入列表顶部 <br> - `YYC3API.savePosts()` 被调用 <br> - 输入框清空 <br> - Toast "发布成功" 显示 <br> - 空内容时按钮禁用 |
| **优先级** | P0 |

### TC-F003: 点赞交互

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 点击心形按钮 |
| **预期结果** | - likes +1 <br> - 数据持久化至 KV <br> - 超过 24 时心形填充为玫红 <br> - `active:scale-125` 动画触发 |
| **优先级** | P1 |

### TC-F004: 频道切换

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 点击不同频道按钮 |
| **预期结果** | - `activeChannel` 状态更新 <br> - 当前频道高亮 (蓝色背景) <br> - (注: 频道过滤未实际实现，仅 UI 状态) |
| **优先级** | P2 |

### TC-F005: 帖子图片展示

| 项目 | 详情 |
|------|------|
| **预期结果** | - 带图帖子正确渲染 `<ImageWithFallback>` <br> - 悬停时图片放大 (scale 1.05, duration 1000ms) |
| **优先级** | P2 |

### TC-F006: 标签与社交同步按钮

| 项目 | 详情 |
|------|------|
| **预期结果** | - 标签以 `#tag` 格式渲染 <br> - 桌面端显示 "同步到: We / XH" 按钮 <br> - 移动端隐藏同步按钮 |
| **优先级** | P2 |

---

## G. 个人中心

### TC-G001: 学习活跃度热力图

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 切换到个人中心 |
| **预期结果** | - `YYC3API.getActivity()` 被调用 <br> - 渲染 168 天 (24 周) + 对齐到周日的热力图 <br> - 颜色梯度: 0=灰, <15=蓝20%, <30=蓝40%, <60=蓝70%, >=60=全蓝+发光 <br> - 方块以 0.001s 延迟依次淡入 |
| **优先级** | P0 |

### TC-G002: 热力图方块点击交互

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 点击任意热力图方块 |
| **预期结果** | - 方块高亮 (ring-2 ring-blue-500 scale-125) <br> - 右侧详情面板显示日期、分钟数 <br> - 显示 1-3 个模拟完成章节 <br> - 学习心得区域显示 (若有笔记) 或"添加"入口 |
| **优先级** | P1 |

### TC-G003: 学习心得编辑与保存

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 点击热力图方块 <br> 2. 点击"添加"/"编辑" <br> 3. 输入 Markdown 内容 <br> 4. 点击"保存心得" |
| **预期结果** | - 编辑区显示 textarea <br> - 保存调用 `YYC3API.saveDailyNotes()` <br> - 切换到展示模式 <br> - `SimpleMarkdown` 正确渲染 `**bold**`, `` `code` ``, `### heading`, ` ```arch ``` ` |
| **优先级** | P0 |

### TC-G004: 架构 DSL 渲染

| 项目 | 详情 |
|------|------|
| **操作步骤** | 1. 输入笔记: ` ```arch\n[Web] -> [API] -> [DB]\n``` ` |
| **预期结果** | - `ArchitectureDiagram` 组件渲染 <br> - 3 个节点框以蓝色边框显示 <br> - 节点间有方向箭头 <br> - 悬停显示 tooltip <br> - 点击节点触发 toast "正在为您检索: ..." |
| **优先级** | P1 |

### TC-G005: 同行对比图表

| 项目 | 详情 |
|------|------|
| **预期结果** | - `useChartReady` 就绪后渲染 AreaChart <br> - 双线: 用户 (蓝) vs 同行 (灰) <br> - 6 周数据点 |
| **优先级** | P1 |

### TC-G006: AI 策略推荐

| 项目 | 详情 |
|------|------|
| **预期结果** | - 基于 peerComparisonData 末位差值: <br> - diff > 20: "高阶领袖策略" (amber) <br> - diff > 0: "持续领先策略" (blue) <br> - diff <= 0: "极速赶超策略" (rose) <br> - 当前数据 diff = 37, 应显示"高阶领袖策略" |
| **优先级** | P2 |

### TC-G007: 成就勋章展示

| 项目 | 详情 |
|------|------|
| **预期结果** | - 4 枚勋章正确渲染 <br> - 悬停放大效果 <br> - 颜色分类正确 (blue/indigo/emerald/rose) |
| **优先级** | P2 |

### TC-G008: 个人资料 Hero 区

| 项目 | 详情 |
|------|------|
| **预期结果** | - 头像 160x160 渐变边框 <br> - 编辑按钮 (右下角) <br> - 铂金会员标签 <br> - 连胜 12 天 (flame 脉冲动画) <br> - 已获证书 05 |
| **优先级** | P1 |

---

## H. 后端 API 端点测试

### TC-H001: GET /health

| Method | URL | 预期状态 | 预期 Body |
|--------|-----|---------|----------|
| GET | `/health` | 200 | `{ success: true, status: "operational", version: "2.0.0" }` |

### TC-H002: GET /user/profile?userId=default

| Method | URL | 预期状态 | 预期 Body |
|--------|-----|---------|----------|
| GET | `/user/profile?userId=default` | 200 | `{ success: true, data: <UserProfile or null> }` |

### TC-H003: PUT /user/profile

| Method | Body | 预期 | 验证 |
|--------|------|------|------|
| PUT | `{ "id": "default", "firstName": "测试" }` | 200 | `data.firstName === "测试"`, `lastActiveAt` 已更新 |

### TC-H004: GET /user/settings?userId=default

| 预期 | 验证 |
|------|------|
| 200 | 返回默认设置或已存储设置, `language: "zh"`, `theme: "dark"` |

### TC-H005: GET /user/progress?userId=default

| 预期 | 验证 |
|------|------|
| 200 | `data` 为数组 (可能为空), 使用 `getByPrefix` 查询 |

### TC-H006: PUT /user/progress/:moduleId

| Body | 验证 |
|------|------|
| `{ "status": "completed", "progressPercent": 100 }` | - progress 记录更新 <br> - `user:profile` 的 `completionPercentage` 自动重算 |

### TC-H007: POST /user/achievements

| Body | 验证 |
|------|------|
| `{ "id": "test-1", "name": "测试勋章" }` | - `earnedAt` 自动添加 <br> - 重复 ID 不重复插入 |

### TC-H008: GET /stats

| 预期 | 验证 |
|------|------|
| 200 | 返回 `DashboardStats` 或默认值 (`activeUsers: 1847`) |

### TC-H009: GET /courses

| 预期 | 验证 |
|------|------|
| 200 | 从 `courses_list` KV key 读取, 默认为空数组 |

### TC-H010: POST /tasks

| Body | 验证 |
|------|------|
| `[{ id: "1", title: "测试", ... }]` | 整体覆盖 `kanban:tasks` KV |

### TC-H011: POST /activity

| Body | 验证 |
|------|------|
| `{ "date": "2026-02-16", "minutes": 30 }` | 累加至 `user:activity:default` 的对应日期 |

### TC-H012: POST /daily-notes

| Body | 验证 |
|------|------|
| `{ "notes": { "2026-02-16": "测试笔记" } }` | KV `user:notes:default` 正确存储, `userId` key 被清除 |

### TC-H013: GET /export?userId=default

| 预期 | 验证 |
|------|------|
| 200 | 包含完整 `DataExport` 结构, 含 `migrationGuide` |

### TC-H014: POST /seed

| Body | 验证 |
|------|------|
| `{ "userProfile": { "firstName": "Test" } }` | 幂等: 仅在 KV 无记录时写入 |

### TC-H015: POST /signup

| Body | 验证 |
|------|------|
| `{ "email": "test@yyc3.com", "password": "..." }` | 创建 Supabase auth 用户 + KV profile |

---

## I. 响应式布局

### TC-I001: Mobile (<=768px)

| 验证项 | 预期 |
|--------|------|
| 侧边栏 | 隐藏, 可通过手势/按钮滑出 |
| 底部导航 | 显示 5 项导航 |
| Header | 简化版 (Logo + 搜索按钮 + 头像) |
| 搜索 | 使用全屏模态框 |
| 内容 padding | `p-6`, 底部预留 `pb-24` |

### TC-I002: Tablet (769-1024px)

| 验证项 | 预期 |
|--------|------|
| 侧边栏 | 隐藏 |
| 底部导航 | 显示 |
| Header | 显示搜索栏 |
| 内容 padding | `p-8` |

### TC-I003: Desktop (>1024px)

| 验证项 | 预期 |
|--------|------|
| 侧边栏 | 固定左侧, 收缩态 `w-20` |
| 底部导航 | 隐藏 |
| 主内容 | `pl-20` 左间距 |
| Header | 完整版 (搜索 + Live 指示器 + 个人信息) |

### TC-I004-I006: Grid 断点 / Touch 优化 / Safe Area 适配 (略)

---

## J. 国际化 (i18n)

### TC-J001: 中文默认语言

| 预期 | `language` 默认值为 `'zh'`，所有 `t()` 调用返回中文 |

### TC-J002: 法语切换

| 操作 | 调用 `setLanguage('fr')` |
| 预期 | 看板标题切换为 "Gestion de Projet" 等 |

### TC-J003: 缺失翻译 Key 降级

| 预期 | `t('nonexistent_key')` 返回 key 本身 |

### TC-J004: 法语翻译覆盖率

| 统计 | zh: 56 keys, fr: 41 keys, **缺失 15 keys** |

---

*文档生成时间: 2026-02-16*  
*测试框架建议: Vitest + React Testing Library + MSW (Mock Service Worker)*
