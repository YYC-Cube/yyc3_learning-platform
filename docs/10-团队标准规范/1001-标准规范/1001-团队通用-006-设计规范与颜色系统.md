**YYC³ Learning Platform Guidelines**
<!--

实现：logo 系统信息  标语，背景照片（可上传）

主题自定义模块，采用图示：十六进制（HEX）

颜色系统（Color System）

1. 颜色表示规范
系统采用两种颜色表示方式，兼顾设计灵活性与开发可读性：
OKLch 颜色空间：用于定义语义化主题变量（如 Primary、Background），通过亮度（L）、色度（C）、色调（H）精准控制色彩，避免传统 RGB 的感知偏差。
十六进制（HEX）：用于具体组件前景色或交互色，直接映射到 CSS 样式，提升开发效率。

2. 语义化颜色变量
系统将颜色划分为多层语义，统一变量；确保视觉一致性与可访问性：


3. 透明度应用
组件阴影使用带透明度的颜色 #d3412b0d（即 Destructive 色的 5.1% 透明度），避免阴影过于突兀，同时保持界面层次感与柔和度。


三、组件样式配置（Component Styling）

所有颜色统一：
OKLch 颜色空间：用于定义语义化主题变量（如 Primary、Background），通过亮度（L）、色度（C）、色调（H）精准控制色彩，避免传统 RGB 的感知偏差。
十六进制（HEX）：用于具体组件前景色或交互色，直接映射到 CSS 样式，提升开发效率。

1. 颜色

主色 OKLch            主前景色 OKLch

次色 OKLch            次前景色 OKLch

强调色 OKLch           强调色前景 OKLch

背景色 OKLch           前景色 OKLch

卡片 OKLch            卡片前景色 OKLch

弹窗 OKLch            弹窗前景色 OKLch

柔和色 OKLch          柔和前景色 OKLch

破坏性 OKLch          破坏性前景色 OKLch
 
边框 OKLch            边框前景色 OKLch

输入 OKLch

---

2.环形区域

OKLch 

图表1 OKLch          图表2 OKLch

图表3 OKLch          图表4 OKLch

图表5 OKLch          图表6 OKLch

侧边栏 OKLch         侧边栏前景色 OKLch
 
侧边栏主色 OKLch      侧边栏主前景色 OKLch

侧边栏强调色 OKLch    侧边栏强调色前景 OKLch

侧边栏边框 OKLch      侧边栏环形元素 OKLch

---
 
3. 字体排版  
      
通过字体家族的分层设计，建立清晰的视觉层次，提升内容可读性与信息区分度，同时支持智能开发工具自动生成排版样式。

无衬线字体

衬线字体

单色字体

---

4. 透明度应用
组件阴影使用带透明度的颜色（即 Destructive 色的 百分比 透明度调整），避免阴影过于突兀，同时保持界面层次感与柔和度。

圆角（Radius）

值：0.5rem       
作用：统一所有组件的边角曲率，符合现代 UI 的柔和设计风格，提升视觉一致性。

阴影（Shadow）   
作用：通过微阴影提升组件立体感，同时避免过度装饰，保持界面简洁。

X 偏移量：0px

Y 偏移量 0px

模糊半径：0px

传播距离：0px

扩散颜色 OKLch

5. 主题色  OKLch 通过亮度调整实现主题浅色/深色调整

---

预设主题：

基础色调

宇宙之夜

柔和流行

赛博朋克

现代极简

未来科技

···
-->
